#!/bin/bash

pcmd="${*:2}"
#echo "$pcmd"
bcmd="cd /root/pcmk/cluster/; setuser $2 mono $(which pash) -c ${*:3}"
#echo "$bcmd"
#rtn=$(cat << EOF
#$(docker exec $1 /bin/bash -lc "$bcmd")
#EOF
#)
rtn=$(docker exec $1 /bin/bash -lc "$bcmd")
echo "$rtn"|sed '1d'
