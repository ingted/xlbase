#!/usr/bin/pash
param(
	$hb64
)

$hd = . decode-mgmt-msg.ps1 $hb64
if($hd.gettype().name -eq "string"){
	$out = $hd 
} else {
	$out = $hd[0]
}

$out | out-file /etc/hosts.tmp -Encoding ascii
$out | out-file ($(pwd).path + "ttc") -Encoding ascii
write-host $out
