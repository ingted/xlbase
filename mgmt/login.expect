#!/usr/bin/expect -f

set host [lindex $argv 0]
set password [lindex $argv 1]

spawn ssh-copy-id $host
exec sleep 10
expect {
	"password:" {
		send "$password\n"
	}
	"already exist" {
	}
}
expect eof
